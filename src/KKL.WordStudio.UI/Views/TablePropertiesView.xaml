<UserControl x:Class="KKL.WordStudio.UI.Views.TablePropertiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Margin="8">
        <TextBlock Text="Table Properties" FontWeight="Medium" Margin="0,0,0,8" />

        <Grid Visibility="{Binding HasSelection, Converter={StaticResource BoolToVisibility}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" Margin="0,0,8,6" />
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Description:" Margin="0,0,8,6" />
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,6" />

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Style:" Margin="0,0,8,6" />
            <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,6">
                <CheckBox Content="Bold" IsChecked="{Binding IsBold}" VerticalAlignment="Center" Margin="0,0,12,0" />
                <TextBlock Text="Font size:" VerticalAlignment="Center" Margin="0,0,4,0" />
                <TextBox Width="40" Text="{Binding FontSize, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Show header:" Margin="0,0,8,6" />
            <CheckBox Grid.Row="3" Grid.Column="1" IsChecked="{Binding ShowHeader}" Margin="0,0,0,6" />

            <Button Grid.Row="4" Grid.Column="1" Content="Apply" Command="{Binding ApplyChangesCommand}" HorizontalAlignment="Left" Padding="8,4" Margin="0,4,0,0" />
        </Grid>

        <Separator Margin="0,12" Visibility="{Binding HasSelection, Converter={StaticResource BoolToVisibility}}" />

        <StackPanel Visibility="{Binding HasSelection, Converter={StaticResource BoolToVisibility}}">
            <TextBlock Text="Binding" FontWeight="Medium" Margin="0,0,0,8" />

            <TextBlock Text="Data source:" Margin="0,0,0,2" />
            <ComboBox ItemsSource="{Binding AvailableDataSourceNames}" SelectedItem="{Binding SelectedDataSourceName}" Margin="0,0,0,6" />

            <TextBlock Margin="0,0,0,2">
                <Run Text="Worksheet: " />
                <Run Text="{Binding ResolvedWorksheetDisplay, Mode=OneWay}" />
            </TextBlock>
            <TextBlock Margin="0,0,0,8">
                <Run Text="Data range: " />
                <Run Text="{Binding ResolvedRangeDisplay, Mode=OneWay}" />
            </TextBlock>

            <StackPanel Orientation="Horizontal">
                <Button Content="Apply Binding" Command="{Binding ApplyBindingCommand}" Padding="8,4" Margin="0,0,8,0" />
                <Button Content="Clear Binding" Command="{Binding ClearBindingCommand}" Padding="8,4" />
            </StackPanel>
        </StackPanel>

        <TextBlock Text="Select a table in the Report Designer to edit its properties."
                   Foreground="Gray" TextWrapping="Wrap"
                   Visibility="{Binding HasSelection, Converter={StaticResource InverseBoolToVisibility}}" />
    </StackPanel>
</UserControl>
